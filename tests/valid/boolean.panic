_fn_ boolean1(x: i32, y: i32) -> bool {
    _return_ (x <= y) && (x < y) && (x == y) && (x != y) && (x >= y) && (x > y);
 }

 _fn_ boolean2(x: i32, y: i32) -> bool {
    _return_ (x <= y) || (x < y) || (x == y) || (x != y) || (x >= y) || (x > y);
 }